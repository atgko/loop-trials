Round 8: addressed all three of round 7's ranked fixes. Varied the five `.marker` section-head icons (target/check/warning/layers, plus one `is-caution` crimson-dark fill on the "Where loops go wrong" marker) instead of repeating `#icon-loop` everywhere; strengthened both watermarks (`stroke-width` 1→3, box 360px→400px and 320px→360px) so they read as a bold background motif instead of a faint outline; the new `is-caution` crimson-dark marker also nudges the crimson-fill ratio slightly toward the 1-in-6–1-in-8 target.
