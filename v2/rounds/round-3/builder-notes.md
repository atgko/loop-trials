# Round 3 — Builder Notes

Switched sections 2 and 4's card lists from a cramped 2-column grid to a single stacked column with larger badges (48px → 56px) and more generous padding (24px → 32px), matching `ref-04`'s spacious proportions instead of the denser packed layout the critic flagged.
