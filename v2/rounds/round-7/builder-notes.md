# Round 7 — Builder Notes

Added a 2px crimson ring (`border`, not a background-fill) to the default `.card .badge` circles, warming up the card-list pattern toward `ref-04`'s crimson-badge treatment without touching the crimson-dominance ratio, since a border isn't a background-color fill.
