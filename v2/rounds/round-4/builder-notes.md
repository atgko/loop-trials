# Round 4 — Builder Notes

Restructured Section 1: moved the diagram earlier and folded the dense third paragraph into a second, trimmed sub-block inside the existing tan `diagram-note` callout, so the section no longer stacks three consecutive plain paragraphs; also gave the "broken check is invisible" card a small caution-kicker label to visually reinforce it as the standout warning.
