# Round 6 — Builder Notes

Split Section 1's two-paragraph `diagram-note` into two separate side-by-side boxed cards (`.note-pair`, a 2-column grid matching the existing `.upside-layout` pattern) and trimmed both notes' prose, directly addressing the "essay-like density" gap flagged in rounds 4 and 5.
