```
Content: 9/10
- Coverage (3/3): All four required beats present as dedicated sections — definition (H2 "What is an agentic loop?", §1), when to use it (H2 "When to use one", §2, four-card checklist), pros (H2 "What loops are good at", §3), cons (H2 "Where loops go wrong", §4). Also reinforced with a worked example (§5) and a takeaway (§6).
- Accuracy (2/2): No invented stats, quotes, or tool names. Claims are conceptual/qualitative ("A loop that runs eight rounds can cost roughly eight times what a single prompt would" is stated as an illustrative ratio, not a cited fact) and internally consistent.
- Audience fit (2/2): Jargon defined inline on first use with bold terms — "An **agent** is different — it's a program that can take actions on its own..."; "**stopping condition** — a rule, checkable by the program itself..."; "Each pass through that cycle is called an **iteration**." No unexplained technical terms.
- Pacing (2/2): Body text totals roughly ~1,500 words across six sections; at a ~150 wpm spoken-lecture pace that lands at ~10 minutes — matches the "10-minute lecture" framing without obvious padding or rushing.
- Engagement (1/1): Concrete worked example in §5 — "a script needs to resize a folder of photos, and a handful of files break the resize" — walked through as a 5-step numbered loop.
- Held at 9 rather than 10 because pacing is an estimate (no actual read-aloud timing was performed), so the "roughly 10 minutes" claim is inferred from word count rather than measured.

Design: 9/10
- Hard gates: all four passed.
  1. Palette: grep of every `#[hex]` in the file returns exactly `#F5F0E6, #FFFFFF, #EAE1D2, #ECE3D3, #BE0000, #890000, #241E1A, #3A332C, #756B60` (lines 9-17, 146, 192, 246, 292, 350) — all nine are on the approved list, nothing else appears. The only non-hex color is `rgba(58, 51, 44, 0.16)` (line 20), which is `#3A332C` at 16% alpha — a tint of an approved color, not a new one.
  2. Emoji/pictograph scan (`[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]`) returned no matches.
  3. Fonts: only `Calibri, "Segoe UI", Arial, sans-serif` (body) and `Cambria, Georgia, "Times New Roman", serif` (headings) appear. Font sizes found via grep: 13, 17, 19, 28, 44 — exactly the approved set, no stragglers.
  4. Contrast (computed via WCAG relative luminance): body-ink `#3A332C` on bg `#F5F0E6` = 10.94:1; ink `#241E1A` on bg = 14.50:1; muted `#756B60` on bg = 4.59:1 (passes the 4.5:1 floor for normal text, used at 13-19px in `.caption`/`.subtitle`/`.takeaway-desc` — tight but compliant); crimson `#BE0000` on bg = 5.80:1; crimson-dark `#890000` on bg = 8.96:1; white on crimson (badges/diagram-box.is-key) = 6.58:1; white on ink (marker/badge circles) = 16.47:1; ink on panel-2 `#EAE1D2` = 12.71:1. All pairs clear their required threshold.
- Polish checks (all hard gates passed) — subtotal: 10/10.
  5. Every margin/padding/gap value found (0, 8, 16, 24, 32, 48, 64, 96px) is a multiple of 8. Pass.
  6. One `border-radius: var(--radius)` (12px) used consistently on every card/box (`.diagram-box`, `.diagram-note`, `.card`, `.grid-square`, `.upside-note`, `.example-card`, `.takeaway-bar`), and one `box-shadow: var(--shadow)` recipe reused on all of them; circular badges use `50%` deliberately as a different shape class, not a competing card radius. Pass.
  7. No gradients anywhere in the file. Pass.
  8. No `<button>`/`<input>` or other interactive controls exist on the page at all, so nothing can look unstyled. Pass.
  9. Every class defined in the `<style>` block (kicker, caption, container, hero*, section*, marker, lead, diagram*, list, card*, upside*, icon-grid, grid-square*, example-card, steps, step*, takeaway*, closing) was traced to at least one matching usage in the HTML. Pass.
  10. Exactly one `<h1>` ("Agentic Loops", line 497); all other headings are h2 (28px) or h3 (19px), visually subordinate. Pass.
  11. Crimson/crimson-dark background fills: `.diagram-box.is-key`, `.grid-square.is-highlight`, `.step-badge.is-final`, `.badge.is-crimson-dark` = 4 "highlighted" elements against ~27 total content blocks (4 diagram boxes + 1 diagram-note + 4+4 cards + 4 grid-squares + 1 upside-note + 1 example-card + 5 steps + 3 takeaway-items) ≈ 14.8%, squarely in the "1-in-6 to 1-in-8" target. The three `.takeaway-bar` crimson fills are thin 6px accent strokes applied uniformly to every takeaway item (not a "pick one out of many" highlight), and this exact pattern — a crimson bar on every item — is what reference image `ref-05-takeaway.jpg` itself does, so it's not counted as a ratio violation. Pass.
- Rendered visual fidelity — adjustment: -1. Full-page screenshot and a viewport crop of the hero were compared directly against the five reference JPGs. The kicker-plus-serif-heading rhythm, the single crimson-filled "highlight" element per group (diagram's "Checks result" box, the "Catches its own mistakes" tile, the final step badge, the "broken check" card) against white/panel-2 cards, the ink-colored circular icon badges, and the off-canvas bleeding loop watermark all read as the same design system as the references — restrained crimson, editorial serif/sans pairing. No overlap, overflow, or misaligned icons were visible anywhere in the render. The one real density mismatch: the reference slides never run more than 2-3 lines of body text in a single block, while this page's `.lead` paragraphs in §1 (two 5-6 line paragraphs) and the two-part `.diagram-note` (also two dense paragraphs stacked with only a `margin-top:16px` label break) read noticeably denser and more essay-like than anything in the five references — a real, visible density gap, not just a spacing-value technicality.
- Final Design score: 10 - 1 = 9/10

Top 3 fixes for next round:
1. Break up the two back-to-back multi-line `.lead` paragraphs in §1 (lines 521 and 523) and the two-paragraph `.diagram-note` (lines 544-547) into shorter chunks or bullet-like fragments — right now they're the densest text blocks on the page and stand out against the terser reference style.
2. In `.diagram-note` (CSS rule at line 201), consider visually separating the "How it repeats" and "Why the check has to be real" sub-notes (currently just a `margin-top: 16px` inline style on the second `.note-label`, line 546) into two distinct note cards so the block doesn't read as one long paragraph pair.
3. Double check that Cambria is actually available in the target rendering environment — the screenshot suggests it may be falling back to Georgia/Times New Roman; if pixel-exact match to the reference's specific letterforms matters, bundle the font rather than relying on the OS having it installed.
```
