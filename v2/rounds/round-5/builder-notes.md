# Round 5 — Builder Notes

Changed the flow-diagram connector arrows from muted gray to crimson (`.diagram-arrow` color), matching `ref-02`'s red-arrow motif that the previous round's critic flagged as the concrete driver of its -1 visual-fidelity deduction.
