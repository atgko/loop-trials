# Round 2 — Builder Notes

Fixed the watermark SVG rendering bug (missing `fill:none; stroke:currentColor` meant the hero/takeaway loop-glyph rendered as a solid black blob) and expanded the prose by ~450 words across all six sections — each addition ties back into the loop diagram or a later section rather than padding — to close the pacing gap from ~7 minutes to ~10.
